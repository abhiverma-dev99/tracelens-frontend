/** Used by Vercel production builds (`ng build`). Local `ng serve` keeps environment.ts. */
export const environment = {
  production: true,
  apiUrl: '/api',
  socketUrl: 'https://tracelens-7hdm.onrender.com',
  projectName: 'TraceLens Frontend',
};
